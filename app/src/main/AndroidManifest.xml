<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.graduation.yinhua.maleambry">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:name=".MaleAmbryApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--欢迎页-->
        <activity android:name=".view.activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--主界面-->
        <activity android:name=".view.activity.MainActivity"></activity>

        <!--登录界面-->
        <activity android:name=".view.activity.LoginActivity"></activity>

        <!--用户信息界面-->
        <activity android:name=".view.activity.UserInfoActivity"></activity>

        <!--用户反馈界面-->
        <activity android:name=".view.activity.FeedBackActivity"></activity>

        <!--我的设置界面-->
        <activity android:name=".view.activity.SettingActivity"></activity>

        <!--修改密码页面-->
        <activity android:name=".view.activity.ModifyPasswordActivity"></activity>
    </application>

</manifest>